the . polutate will fetch docuements of both the collectionsi

i.e both model data

modelOuter.find({}).popullate("typeee")  
can be accessed by modelOuter.typeee()

where modelOuter :{{
typee : {

    }

}}
